node_modules/
dist/
.cache/
models/

# Local tool state (oh-my-claudecode / oh-my-open-code / zcode) — not part of the repo.
.omc/
.omo/
.zcode/

# Generated indexes (default location is <db>/.mdss). These are derived
# artifacts - rebuild with `mdss index`. Ignore any that land in-repo.
.mdss/
*.mdss-index.json
vectors.json
.hashes.json

# npm pack artifacts (produced by scripts/verify-pack.mjs on failure) — never commit.
*.tgz
